function AddEntry()
{
    //create a form that allows the user to add an entry. Then have a submit button that onced clicked,
    //adds the entry to the array of objects

    return (
        <div className="flex justify-center items-center">
            <div className="h-12 w-7/12 bg-gray-200 text-white mb-20 rounded-lg">
                hi
            </div>
        </div>
    )
}

export default AddEntry;